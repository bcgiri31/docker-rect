sudo: required
services:
  - docker

before_install:
  - docker build -t test1 -f Dockerfile.dev .

script: 
  - docker run -e CI=true test1 npm test -- --coverage

deploy:
  provider: elasticbeanstalk
  region: "us-east-1"
  app: "react1"
  env : "React1-env"
  bucket_name: "elasticbeanstalk-us-east-1-379093691113"
  bucket_path: "react1"
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY



